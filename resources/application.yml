spring:
  datasource:
    url: jdbc:mysql://localhost:3306/osemes_orders
    username: root
    password: #mySQLpwd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

